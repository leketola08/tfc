  mongo-express:
    image: mongo-express
    container_name: mongo_express_container
    restart: always
    environment:
      ME_CONFIG_MONGODB_ADMINUSERNAME: ${MONGO_INITDB_ROOT_USERNAME} # Use the same user
      ME_CONFIG_MONGODB_ADMINPASSWORD: ${MONGO_INITDB_ROOT_PASSWORD} # Use the same password
      ME_CONFIG_MONGODB_SERVER: mongodb # Use the container name
      ME_CONFIG_MONGODB_URL: mongodb://${MONGO_INITDB_ROOT_USERNAME}:${MONGO_INITDB_ROOT_PASSWORD}@mongodb:${MONGO_PORT}/?authSource=admin # Correct URL
    ports:
      - "${EXPRESS_PORT}:8081"
    depends_on:
      - mongodb
    networks:
      - mongonetwork

networks:
  mongonetwork:
    driver: bridge
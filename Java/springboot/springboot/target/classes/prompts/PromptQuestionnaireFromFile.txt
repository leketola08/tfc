Dado el siguiente contexto:

{context}

Basándote en este contexto, genera {numquestions} preguntas para un cuestionario. Las preguntas deben estar en español y seguir la siguiente estructura JSON
 no añadas campos nuevos, solamente puedes dejar valor por defecto diciendo que no lo sabes:

{structure}

Crea un título para todo el cuestionario suele estará al principio del contexto, que si no es encontrado por defecto es 'Cuestionario-{datetimetoday}'.
Una descripción que es más largo que el título que provee más contexto sobre el cuestionario, que si no es encontrado es el mismo que el título.

Sigue estas reglas para la generación de cada pregunta:
- Texto de la pregunta: Formula una pregunta clara y concisa en español que se pueda responder basándose en el
contexto proporcionado. No repitas la misma pregunta. Debes diferenciar que preguntas son de tipo ejemplo o teóricos. Si es un ejemplo el texto debe empezar por ej:
- Tipo: Determina el tipo de pregunta más apropiado:
    - "MO" para preguntas de opción múltiple sin un orden específico
    - "OR" para preguntas de opción mútiple con un orden específico como ordenar los pasos de un proceso por ejemplo
    - "BOOL" para preguntas de verdadero/falso o si/no
    - "LA" para preguntas abiertas largas
    - "SA" para preguntas abiertas cortas
     Determina el tipo según la información del contexto y cómo se podría responder la pregunta. Intenta variar los
     tipos de preguntas.
- Respuestas: Si el tipo de pregunta es "MO" o "OR" genera como mínimo 3 respuestas plausibles pero con preferencia a 4
"BOOL" generada para respuestas de verdadero/falso solo tiene 2 opciones. Para "LA" y "SA", la lista de respuestas estará vacía.
- Respuesta correcta: Para "MO", "OR", y "BOOL", identifica y proporciona la respuesta correcta basada en el
contexto. Si la respuesta no es directamente obvia, indica la respuesta más probable o deja el campo como `null` si no
se puede determinar con certeza. Las respuestas abiertas solo tienen una respuesta correcta o NULL
Marca todo en MAYUSCULAS si no encuentras una respuesta clara
Asegúrate de que la salida sea un JSON String válido.